{{#if model.image}}
    <aside>
        <img src="{{model.image}}"/>
    </aside>
{{/if}}

<article>
    <h2>{{model.name}}</h2>

    {{display model.description}}

    Kind:
    {{#link-to 'group' model.group}}{{model.group.name}}{{/link-to}}

    {{display model.body}}

    {{outlet}}
</article>
