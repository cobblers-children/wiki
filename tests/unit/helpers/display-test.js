import { display } from '../../../helpers/display';
import { module, test } from 'qunit';

module('Unit | Helper | display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = display(42);
  assert.ok(result);
});
